[project]
name = "remarkable-bridge"
version = "0.1.0"
description = "Bidirectional sync between Markdown vault and reMarkable tablets"
requires-python = ">=3.12"
dependencies = [
    "httpx",
    "pillow",
    "rmc>=0.3.0",
]

[project.optional-dependencies]
dev = []

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
